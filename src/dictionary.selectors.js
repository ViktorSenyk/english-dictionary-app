export const storedWordsSelector = state => state.dictionary.storedWords;
export const historySelector = state => state.dictionary.history;
// export const randomWordSelector = state =>
//   storedWordsSelector(state)[Math.floor(Math.random() * storedWordsSelector(state).length)];
